#include<stdio.h>
void main()
{
    int n1,n2,i,j,fact;
    scanf("%d %d",&n1,&n2);
    for(i=n1+1;i<n2;i++)
    {
        fact=0;
        for(j=2;j<=i/2;j++)
        {
            if(i%j==0)
                fact=1;

        }

    if(fact==0)
                    printf("%d ",i);
    }
}
